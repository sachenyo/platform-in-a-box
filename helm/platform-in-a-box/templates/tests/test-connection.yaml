apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platform-in-a-box.fullname" . }}-test-connection"
  labels:
    {{- include "platform-in-a-box.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platform-in-a-box.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
