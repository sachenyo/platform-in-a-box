apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-in-a-box.fullname" . }}
  labels:
    {{- include "platform-in-a-box.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platform-in-a-box.selectorLabels" . | nindent 4 }}
